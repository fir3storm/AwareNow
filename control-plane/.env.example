# Local build/test defaults. Do not place a production secret in this file.
PORT=3001

# Optional settings are validated when supplied but are not used to provision
# infrastructure or control an engine in this foundation increment.
DATABASE_URL=postgresql://awarenow:replace-with-secret-reference@localhost:5432/awarenow
CONTROL_PLANE_BASE_URL=http://localhost:3001
AWARENOW_CONTROL_TOKEN=replace-with-managed-secret-reference
